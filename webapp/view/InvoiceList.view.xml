<mvc:View
   controllerName="cadaxo.app.controller.InvoiceList"
   xmlns="sap.m"
   xmlns:mvc="sap.ui.core.mvc">
   <List
      headerText="{i18n>invoiceListTitle}"
      class="sapUiResponsiveMargin"
      width="auto"
      items="{invoice>/Invoices}" >
      <items>
         <ObjectListItem
            title="{invoice>ProductName}"
            number="{
			   parts: [{path: 'invoice>ExtendedPrice'}, {path: 'currency>/currency'}],
			   type: 'sap.ui.model.type.Currency',
			   formatOptions: {
				   showMeasure: false
			   }
		    }"
		    numberUnit="{currency>/currency}"
		    markFavorite="true"
			markFlagged="true"
			markLocked="true"
			showMarkers="true"
			intro="{invoice>Intro}"
			icon="{invoice>Icon}">
		    <firstStatus>
					<ObjectStatus text="{
						path: 'invoice>Status',
						formatter: '.formatter.statusText'
					}"
					state="{
						path: 'invoice>Status',
						formatter: '.formatter.statusState'
					}"/>
				</firstStatus>
			</ObjectListItem>
      </items>
   </List>
</mvc:View>